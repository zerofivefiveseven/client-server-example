# Template for client & server app

The client contains:
* The implementation of consumer-producer pattern.
* Data processing (number processing as a placeholder).

The server:
* Receives and validates requests.

IPC is implemented via unix sockets.